# 版本历史

[返回首页](README.zh-CN.md) · [English](CHANGELOG.md) · [下载与发布说明](https://github.com/LumiaBlack51/twodrive/releases)

本页整理自原 README。发布资产和验证说明保留在 Releases；[debian/changelog](debian/changelog)保留软件包历史。以下描述的是发布时的变化，不代表新的测试结果或当前行为保证。

## 0.2.9 — 修复解压时的 I/O 错误

新建本地文件时，缓存路径和写入状态现在一次性写入数据库，不再经过云端元数据过滤逻辑。向等待云端创建或移动完成的目录中解压文件不再因此报 I/O 错误；云端更新仍会等待父目录操作完成。包含之前尚未发布的 Nautilus 快捷方式后台解析修复，避免在界面线程探测文件系统。

详情：[解压故障调查（英文）](doc/archive-extraction-2026-09-15.md)。

## 0.2.8 — 无需注册应用即可登录

内置 TwoDrive 公共 Microsoft 应用 ID，新用户可直接登录。读取配置时，缺失、空白和旧版占位 ID 自动使用内置应用；保留自定义注册支持。组织同意策略仍可能要求管理员批准。

## 0.2.7 — 释放已固定内容

“释放空间”现在会先取消所选文件或文件夹子树的显式和继承固定策略，再安全释放缓存。已释放子项在固定父目录下仍保持仅云端。自动清理继续保护固定内容；上传和打开的文件句柄仍会延后释放，确保数据安全。

## 0.2.6 — 保存与移动可靠性

修复旧空占位文件的上传确认覆盖已完成下载、PDF 保存时间戳变化及备份式保存的同步竞争。子文件上传和元数据操作等待父目录移动完成；子项待办路径随本地移动更新，旧云端元数据不再将待处理子项拉回原路径。Graph 返回负数目录大小时，不再导致元数据解析中断。

已有云端同名目录仍需单独核对；本版本不会自动合并或删除它们。

详见：[目录移动](doc/folder-move-sync-2026-09-11.md)、[下载完成竞争](doc/download-placeholder-race-2026-09-10.md)、[PDF 保存](doc/pdf-save-reliability-2026-09-09.md)。

## 0.2.5 — 释放空间会取消下载

释放未固定文件会取消下载并移除未完成片段；文件夹操作覆盖子项。旧句柄不会重启已取消的传输，主动重新打开文件可以再次下载。传输和退避时检查取消，阻塞中的请求需要返回或超时后才能结束。

上传仍保留延后释放保护：云端确认前保留未上传数据。“始终保留”的保护不变。

详见：[取消语义与验证](doc/download-cancellation-2026-09-08.md)。

## 0.2.4 — 目录浏览与延后释放

Nautilus/GIO 探测未知扩展名的类型时，不再仅为识别类型而下载整个仅云端文件。浏览使用 SQLite 元数据，不增加文件头或内容预取缓存；未知格式可暂时显示通用图标。主动读取和复制仍会下载内容。

写入或上传过程中申请释放，显示“云朵＋同步”，父目录也汇总该状态。上传成功且缓存安全移除后变为仅云端；错误和冲突不显示为释放完成。

详见：[原因与验证](doc/directory-browsing-2026-09-08.md)。

## 0.2.3 — 响应性与恢复

先由本地元数据提供挂载，再恢复网络任务。目录快照和后台读取减少传输对浏览的阻塞，Nautilus 状态查询异步执行。临时网络故障保留可续传的上传会话。常用文件夹只上传处理支持重试，不额外复制本地内容，也不传播本地删除。

详见：[响应性与恢复记录](doc/responsiveness-recovery-2026-09-08.md)。

## 更早的历史

参见 [debian/changelog](debian/changelog)、[Releases](https://github.com/LumiaBlack51/twodrive/releases) 和[历史工程记录](doc/README.md#engineering-notes)。
