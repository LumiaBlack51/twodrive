# TwoDrive 维护指导

## 每次故障必须留档

从 2026-09-15 起，处理本仓库的任何故障（包括复发、用户报告和维护中发现的缺陷）时，必须执行以下流程：

1. 开始排查前阅读 `doc/incidents.md` 和与症状相关的历史调查，确认是否存在相同触发条件或相邻机制。不能仅凭错误提示相同就认定根因相同。
2. 在 `doc/incidents.md` 中新增故障条目，使用该文件的模板。尚未定位时写明“调查中 / 根因未确认”，不能为了填满记录而猜测。
3. 记录症状、影响版本、触发条件、根因与证据、解决办法、实际验证结果、验证边界和防复发措施。修复之外的本地恢复操作要单独说明。
4. 对可复现的软件缺陷补充有意义的回归测试；条件允许时证明“旧代码失败、修复后通过”。无法复现或无法运行测试时，如实记录原因和未覆盖范围。
5. 再次出现相关故障时新增条目并关联旧条目，解释是原修复遗漏、代码回归还是另一种根因。保留旧结论；新证据推翻旧结论时追加更正及日期。
6. 提交修复代码或发布前同步更新记录，记录应随修复一起提交。调查未结束时也要保存已有证据和下一步。提交哈希、发布链接只能填写已存在且核实过的值，尚未产生时标为待补充。
7. 最终回复区分“确认修复的机制”和“尚未验证的范围”，并提供记录文件链接。不能把模拟云端测试说成真实 OneDrive 端到端验证，也不能承诺所有同类错误永久消失。

故障记录不得包含令牌、密钥、上传会话 URL、完整私人文件清单或其他敏感数据；日志只保留必要且脱敏的片段。

复杂故障可以另写 `doc/` 下的详细调查，但仍须在 `doc/incidents.md` 留下摘要和链接。其他开发与验证要求见 `CONTRIBUTING.md`。
